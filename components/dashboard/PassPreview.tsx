'use client';

import React from 'react';

interface PassPreviewProps {
  mode: string;
  data: any;
}

const PassPreview: React.FC<PassPreviewProps> = ({ mode, data }) => {
  // Your component logic here
  return <div>{/* Render your preview here */}</div>;
};

export default PassPreview;
